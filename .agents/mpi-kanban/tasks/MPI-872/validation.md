# MPI-872 Validation

**Verify mode:** `user-ux` — the only check that counts is a real click landing on a real
product page with the discount applied. Reading config proves nothing here.

(Empty until the work runs.)
