# MPI-753 checklist

- [x] Rename the Model Library filter label `Size` to `Tier` (`MpiModelManager.js` template)
- [x] Confirm no doc, test or other string depends on the old label
- [ ] Reload the Model Library and see TIER beside LOW / BALANCED / HIGH (left to Fabio's own app; the renderer is served from the tree, so a reload picks it up)
