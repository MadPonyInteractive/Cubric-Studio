# A masked edit shows the model only the mask box, so it cannot keep what is outside it

- [ ] Decide the direction: crop context factor, or the whole picture in a free reference slot.
- [ ] Apply it on ONE model first and render the MPI-877 ask: the boy's reflection alone turns demonic, under a normal boy.
- [ ] Check every other masked op on that graph still stitches cleanly (the crop is what `InpaintStitchImproved` puts back).
- [ ] Fabio judges the render.
