# MPI-748 — Head Swap on Klein follow-ups (umbrella)

Created 2026-09-13 at Fabio's request over the four cards filed in the BFS face swap session.
Itself a member of MPI-560 (the Flow track). Carries no code of its own.

**Work mode: ONE card per session, `/mpi-handoff` between them, in the phase order below.**
No `## Parallel Batch`: MPI-746 and MPI-747 own disjoint files and could run side by side, but
Fabio chose sequential. Each member's description holds its full evidence and scope — read the
card, not this summary, before starting its phase.

## Members

| Card | What it is | Phase | State at creation |
|---|---|---|---|
| MPI-746 | Klein samplers: every shipped Klein graph and Flow from `euler` to `lcm` | 1 | todo |
| MPI-747 | `Output_Display`: a display-only graph output shown on a Flow's final stage | 2 | todo |
| MPI-744 | Klein Head Swap Flow: Klein 9B/4B + BFS head LoRA replaces Qwen | 3 | doing (bench) |
| MPI-745 | LTX 2.3 video head swap, BFS IC-LoRA v3 | — | todo, **deferred** |

Adjacent, deliberately NOT a member: **MPI-348** (Krea2 BFS bench, `doing`) stays under MPI-560
phase 4 — it was picked up in the same session but not filed in it.

## Phase 1: MPI-746 — lcm everywhere

First because it is the smallest, depends on nothing, and is already bench-proven on t2i
(6 same-seed generations: lcm removes euler's extra limbs). MPI-744's graph is born on lcm, so
the order costs it nothing.

Ownership (seed `files.json` with this at pickup):
`comfy_workflows/raw/klein_t2i_template.json`,
`comfy_workflows/scripts/workflow_generation/klein_t2i_template.json`,
`comfy_workflows/klein_t2i.json`, `comfy_workflows/klein_9b_t2i.json` (both REGENERATED by
`generate_klein.py`, never hand-edited), `comfy_workflows/raw/flow_draw_it_in.json`,
`comfy_workflows/raw/flow_object_stamp.json`, `comfy_workflows/raw/flow_scribble.json` and their
runtime twins in `comfy_workflows/`, `docs/models/klein/README.md`, `docs/models/klein/removal.md`.

Gate: the card's VERIFY. The LanPaint removal sampler (node 652) is decided by its own A/B, not
flipped with the rest. Same-seed comparisons need Fabio's eyes — ask, do not self-judge images.

**Drift at pickup (2026-09-13):** two more Klein samplers carried `euler`/`beta`, missed by the
card's scan: detail `MaskDetailerPipe` 415 and upscale `UltimateSDUpscale` 405. Fabio benched
`euler`/`beta` vs `lcm`/`simple` vs `lcm`/`normal`: **`lcm`/`normal` wins** wherever a node has a
scheduler widget. LanPaint 652 cannot follow — `LanPaint_KSampler`'s own sampler list has no `lcm` —
so Fabio benched SDXL's LanPaint config and it won: **`euler_ancestral`/`simple`**. Phase 1
landed on disk and green; next is phase 2, MPI-747. `removal.md` was NOT edited: it is marked history, and its `euler`
line records the 2026-07-26 measured config.

## Phase 2: MPI-747 — Output_Display

Before MPI-744 because the Klein Head Swap graph carries the display node: wiring the Flow first
would ship a result pane that ignores it.

Ownership: `js/services/commandExecutor.js`, `js/components/Organisms/MpiBaseFlow/MpiBaseFlow.js`,
`docs/playbooks/add-flow/ui/result-pane.md`, plus one new test (name it at pickup).

Decision to confirm with Fabio at pickup: display wins the first paint and the existing surface
toggle still reaches compare and the player (the card's PROPOSED default).

Gate: the card's VERIFY — exactly one gallery card per run, unchanged Flows without the node,
reopen shows the display and falls back to the result after an engine restart.

## Phase 3: MPI-744 — the Klein Head Swap Flow

Prerequisites, all Fabio's at the bench (tracked in MPI-744's checklist): the 9B rank128 vs
rank64 A/B, 4B `v1` vs the undocumented `v1.1_optional`, crop-stitch or full frame, and the graph
exported to `comfy_workflows/raw/flow_head_swap.json` WITH `Output_Display` and on lcm.

Route: `/mpi-add-flow` and `docs/playbooks/add-flow/existing-flows/head-swap.md` — an existing
Flow re-engined, so its descriptor changes rather than a new one being added.

Expected ownership: `comfy_workflows/raw/flow_head_swap.json` + runtime, `js/data/flowsRegistry.js`
(the `head-swap` entry), `js/data/modelConstants/loraDeps.js`,
`docs/playbooks/add-flow/existing-flows/head-swap.md`.

Known work inside the phase:
- New BFS Klein LoRA deps + R2 upload. **Keep the `qwen-lora-headswap` entry** — the orphan
  sweep reads DEPS, so deleting it strands 1.2GB on users' disks
  (`docs/playbooks/add-model/README.md` § Removing).
- `requiredModels` moves off `qwen-edit`; a tier that picks the Klein size makes it an any-of
  case (`docs/playbooks/add-flow/any-of-models.md`).
- `Input_Tier` options, labels and relative costs are re-measured for 9B / 4B.
- `comfyui-inpaint-cropandstitch` stays in `requiredDeps` only if the new graph still calls it.
- The tile and hero were cut from a Qwen run: re-cut via `/mpi-flow-graphics` or keep — ask.

## Deferred: MPI-745

Picked up only after phases 1–3 land, and Fabio may reject it (a more complex workflow). The card
holds the LTX-2 / 2.3 / 2.5 version map and the silent-load trap.

## Closing the umbrella

When phase 3 lands, ask Fabio once: close MPI-745 as `rejected`, or keep it — and with it this
umbrella — open. The umbrella moves `todo -> done` when its last kept member does.

## Resume here

**Phase 1 (MPI-746) is DONE and committed** (2026-09-13), with its `UNRELEASED.md` line.
**Fabio asked for the Flow head swap (MPI-744) next.** This plan puts phase 2, MPI-747
(`Output_Display`), first because the head swap graph carries the display node — confirm the
order with Fabio at pickup, and MPI-744 still needs his exported raw graph.
