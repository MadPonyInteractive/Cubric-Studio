# MPI-802 Checklist

- [x] `ask` defaults ON (default const + normalizer)
- [x] `stageOnConnect` defaults ON (default const + normalizer)
- [x] Settings copy says the new default
- [x] Fresh-store check run (node + stubbed localStorage)
