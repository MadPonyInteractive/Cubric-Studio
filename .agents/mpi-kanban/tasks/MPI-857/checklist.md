# MPI-857 — checklist

- [ ] `Duplicate frame` in the strip's right-click menu, beside Delete
- [ ] The copy carries the source frame's cut-out mask (fresh order token, no `_viewerPos` collision)
- [ ] Staged only: the pill rises, nothing reaches the server until Update/Apply
- [ ] A multi-thumb Ctrl-click selection duplicates each frame after itself
- [ ] `tests/desktop/gif-workspace.spec.js` covers the new row and the staged insert
- [ ] `docs/gif.md` + `js/components/types.js` name the third menu row
