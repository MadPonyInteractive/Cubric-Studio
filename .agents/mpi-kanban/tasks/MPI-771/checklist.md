# MPI-771 Checklist

Derived from `tasks/MPI-757/plan.md` (2026-09-16).

- [x] Engine half (MPI-757 Batch 2 worker)
- [x] Engine verify: tests/gif-cutout.test.cjs + one real local track
- [x] UI half (MPI-757 Batch 3)
- [x] Redesign per Decision 14 (Phase 3b): base layer, per-frame masks, Mask Brush, Track All / Single Frame - automated checks green
- [x] Fabio's six findings from the first eye check (2026-09-16 13:10Z) - automated checks green
- [x] Fabio's second check (2026-09-17): held thumb overshoot, native-drag "copy" + stuck Discard, preview button in the Mask Brush - automated checks green; Discard stays frames-only (Fabio: a)
- [x] types.js typedef hunk + `preloadStyles.js` — landed 2026-09-18 once MPI-774's claim no longer covered either file; rewritten against the code as it is, `types-hunk.md` deleted
- [x] UI verify: local engine AND RunPod (user-ux) - local 2026-09-17 ("1"), RunPod 2026-09-17 (Fabio's app, RTX 2000 Ada)
- [x] Header ENTRIES count refetches after a GIF tool Apply (found in the RunPod check screenshot)
- [x] Fabio's UI pass (2026-09-17 ~14:00Z): Cut out saves transparent; `colours` stored as 0 (3 routes); checkerboard behind frames; spinner + status bar while masking; masks survive Cut out (per-list stash); "name what to KEEP" hint; cut-out records its settings
- [x] Mask methods: Remove background (BiRefNet, default), By name (SAM3), By colour (shared `js/utils/colourKeyMask.js`); runner hunk waits on MPI-774's commandExecutor.js claim (message 4463a29e)
- [x] By colour in the IMAGE mask tools (Fabio, 2026-09-17)
- [ ] Fabio's UI check of the above after a full app restart
- [x] Fabio's second pass (2026-09-18 ~11:3xZ), items 1-4: strip right-click menu (Delete frame / Clear this frame's mask); the trim range painted on the strip + the Trim note telling the truth on a full range; the gallery hover-play artefact root-caused (transparent GIF over an unhidden, differently-scaled poster) and fixed in CSS; the `[data-info]` gap closed on the control bar's trim handles + frame counter
- [x] Item 5: the Background tint question — RESOLVED without the screenshot, see the tint-rule line below. No screenshot needed
- [x] Fabio's call on the RAIL: yes, on the status bar; no floating tooltips. Built, see below
- [x] Fabio's eye pass on items 1-4 (2026-09-18): context menu PASS, Trim PASS ("I like the green indicators"), gallery multi-entry PASS
- [x] **Backspace does not delete a frame in his app — ROOT-CAUSED AND FIXED (2026-09-18).** Fabio confirmed the orange ring, so selection was never the fault. `hotkeyManager._normalizeKey()` builds `control+backspace` while the selection modifier is still held, and the registry carried only bare `backspace`, so `_handle` returned at the `handlers` lookup before any handler ran — silently. Three registry ids now cover bare / ctrl / shift; the spec's press carries `ctrlKey: true` and was PROVEN RED on the pre-fix files first
- [x] The rail says what each tool does ON THE STATUS BAR, never as a floating tooltip (Fabio, 2026-09-18) — a `desc` per GIF rail entry. The wrap keeps the NAME (the rail's own `mouseover` tooltip reads the wrap), and MpiButton's `info` carries the sentence, because statusBar.js resolves `closest('[data-info]')` and so never sees the wrap. Every existing spec selector targets the wrap by name and still matches
- [x] ONE tint rule everywhere: THE TINT IS WHAT GOES (Fabio, 2026-09-18) — `flip = !_invert`, and the per-method `#tint-note` badge deleted. This also CLOSES the "Background tint over the background while the cut kept the robot" question: no polarity bug, it was the old "tinted = what stays" semantics read the natural way
- [x] Scope consolidation (Fabio, 2026-09-18): four buttons became **Mask** + **Clear** with an All / Frame / **Selected** `MpiRadioGroup`. Selected is the strip's Ctrl-click set, reaching the panel through a new `selection-change` event + `setSelection()`. Found on the way: the scope radio could visually diverge from `_scope` when a run was busy — it locks with the method radio now
- [x] The re-key TOAST is gone (it fired on every paused slider drag). The picker's and Tolerance's `info` say "Changes are only visible once you press Mask" on the status bar instead
- [ ] Fabio's eye pass on the six items above
- [ ] **Mask display consistency (Fabio, 2026-09-18) — investigated, NOT built, needs his scope call.** The GIF Mask Brush already mounts the image `MpiMaskStrip` (invert, B/W view, opacity, clear) and `MpiGifViewer` implements all of it. The gap is the CUT-OUT TINT, a read-only preview at a fixed `--accent-heat` 0.45 with no controls at all
- [x] **Consistency audit finding 4 (2026-09-19): the GIF STAGE joined the shared context menu.** `MpiGifViewer` emits `gif-viewer:context-menu` the way `MpiVideoViewer` and `MpiCanvasViewer` do; the Block's `isGif` branch owns the items (Save frame as image / Reverse frames / Clear all masks, the last dead with no masks). Frame-scoped verbs stay on `MpiFrameStrip`'s own menu
- [x] **Finding 3: the two panels that were nothing but an Apply are gone from the rail.** `gifReverse` + `gifSaveFrame` removed from `GIF_TOOLS`, `TOOL_OPTIONS_REGISTRY`, `_GIF_TIMING_TOOLS`, `_GIF_TRANSFORM_TOOLS`, `TOOL_LABELS` and both panels' own `TOOLS` tables. Their handlers are untouched - the context menu calls them. `gifTrim` keeps its panel: its note is real UI, not just an Apply
- [x] Verify: `lint` + `lint:components` clean; the new `gif-workspace.spec.js` stage-menu test PROVEN RED with `MpiGifViewer.js` + `MpiGroupHistoryBlock.js` restored from HEAD, then 3/3 green
- [x] **Finding 1: Cut-out mounts the shared MpiMaskStrip.** It now calls `enterMode('mask')` and mounts `MpiMaskStrip({ viewer, brush: false })`, the same component the Mask Brush and every image mask tool mount - opacity, invert display, B/W view, clear. Fabio re-confirmed the rule (*"everything that's masked is what's supposed to disappear in any world"*), and `setMaskInverted` is NOT a geometric complement (`MpiCanvas.js:972` recolours the mask black), so the canvas is fed the already-flipped bitmap through a new `MpiGifViewer.setCutoutPreview()` display override. Read-only by construction. Clear routes to `clearFrameMasks(index)` under the override. Consequence: Cut-out is a canvas tool, so the built-.gif preview toggle is off in it like the Mask Brush, and the stage shows one frame until Play
- [x] **Finding 2: CLOSED by Fabio, no change.** *"I don't care about timing anymore. Leave it as it is."* Timing keeps its group
- [ ] Finding 5: the GIF output tool gets the GIF Maker's preview pane. Needs no decision
- [ ] Fabio's eye pass on the WHOLE workspace (nothing closes before it)
- [x] Fabio PASSED the context menus (2026-09-19): *"Menus are good, and context looks good. The context pop-up looks good."*
- [x] Verify (finding 1): the strip assertions in `gif-cutout.spec.js` test 1 PROVEN RED with the panel, viewer and Block restored from HEAD, then green; gif-cutout 1/3/4 + gif-workspace 3/3 + mask-colour 1/1 green; lint + lint:components clean; `node --test` 1372 pass, the only 2 failures being peer live-model harnesses with no DEEPINFRA_API_KEY
- [x] **SPACE belongs to the canvas while a canvas tool is up (Fabio, 2026-09-19).** He spotted it: hold-Space is the only pan, so Space cannot also play. Root cause, not a symptom: `canvas.pan.start` and `video.playPause` are two registry ids on ONE key and `hotkeyManager` buckets handlers by `type:key`, so BOTH fired - one press panned and toggled playback. Pre-existing in the Mask Brush; Cut-out inherited it by becoming a canvas tool. `MpiGifControlBar` now stands its play hotkey down on `edit-change` (flag reset in `detachViewer`). Play BUTTON unaffected, arrows uncontested. Proven RED with only the bar restored from HEAD. `docs/video-player.md` corrected - it claimed the two bars never compete, which was true of the bars and never true of `space`
- [x] **Finding 5: the GIF output tool gets the GIF Maker's preview pane.** New `POST /gif/preview` runs the SAME `buildGif()` Apply runs (a second encoder would make the pane a decoration), to ONE file per project (`Media/.gif-preview/preview.gif`), mtime-busted, no sidecar and no sweep. `MpiToolOptionsGifTiming` gains the pane in `output` mode only - REMOVED, not `[hidden]`, on the other three (MPI-382). Encoder injected by the Block, the `MpiToolOptionsGif` division. Badge goes stale on a settings change rather than the pane clearing. Proof: `tests/gif-preview.test.cjs` 3/3 (real frames, real encode, nothing reaches the history, one file, bad bodies refused) + a new `gif-workspace.spec.js` pane test
- [x] **MASTER WENT RED ON MY COMMIT (`756cf0e0`) AND IS FIXED (`3efcdc9f`).** Only red on master, everything before it green; `gif-cutout.spec.js` test 2, "the brushed corner is still kept", 3 runs. Root cause: `_editIdx` means "the canvas holds this frame's real layers and may be saved back", not "the frame on the canvas". Cut-out's canvas holds neither under the display override, so a landing `setTrackMask` -> `_refreshEditBase()` -> `_saveEdit()` wrote EMPTY brush layers over a real fix. Stays -1 in BOTH claim sites now. Guarded by a new `gif-workspace.spec.js` test, proven RED against `git show 756cf0e0:MpiGifViewer.js`
- [x] CI GREEN after the fix: run 35439088934 on `3efcdc9f`, 128 passed, including `gif-cutout.spec.js:446` (the spec that was red) and both new tests
- [x] **Fabio's SECOND pass (2026-09-19 late) - ALL FIVE BUILT** (session `5e86d76c`). Each was already root-caused when handed over; none needed re-investigating. Detail per item in plan.md -> Remaining Work -> "Fabio's SECOND pass"
- [x] **(1) Shift-selects a RANGE on the frame strip, Ctrl still toggles one.** `MpiFrameStrip` pointerdown splits `modifier` into `range`/`toggle`; pointerup grew `_rangeSelect()` walking `_anchor` -> idx. Found on the way and fixed: the PLAIN-click branch never repainted, because it emits `frame-select` and lets the Block call back into `setCurrentIndex`, which early-returns when the index has not moved - so clicking the frame you were already on left the old selection painted
- [x] **(2) The mask display toggles carry between Cut-out and the Mask Brush.** Fixed the READ, not the 300 ms global debounce: `projectService` exports `getPendingToolSettings(toolKey)` (the queue's un-flushed partial) and `MpiMaskStrip` layers it over `getToolSettings` at mount
- [x] **(3) Cut-out panel order.** `#strip-slot` moved above `#cutout-slot` into a `__section` with a `Mask Preview` label; Cut out is last. No CSS needed. Folded in: message `e93c9db7` (MPI-736) - Stop's variant `danger` -> `primary`
- [x] **(4) No white flash leaving a masked run.** `_setTint(null)` now hides ONLY: `--luma` and the mask image stay until the next mask replaces them, so the opacity fade never runs in `mask-mode: alpha` over an opaque alpha-255 bitmap
- [x] **(5) Space PLAYS in Cut-out and still pans in the Mask Brush.** `edit-change` carries `ownsDrag` (`crop || paintEnabled`), re-emitted by `setMaskPaintEnabled` because the strip mounts AFTER `enterMode`; the bar gates the play hotkey on that, not on `editing`. `gif-cutout.spec.js` asserted the OLD rule and was updated, not deleted
- [x] Verify (second pass): `lint:components` clean; `node --test tests/*.test.cjs` 1408 pass / 0 fail; desktop gif-workspace 5/5 + gif-cutout 3/4, the one failure being the peer frame-store bug at `gif-cutout.spec.js:446`, re-proven not ours by running it with ALL SIX of this session's source files put back to HEAD
- [x] Each of items 1, 2, 4 and 5 PROVEN RED before being believed, one file at a time: frame strip alone -> item 1 returns `['3']`; mask strip + projectService -> item 2 false; control bar -> item 5 false; a surgical revert of `_setTint`'s body -> item 4 false
- [x] **Fabio's THIRD pass (2026-09-19) - both items built.** He checked the second pass in the app: the invert toggle "does not carry between tools yet", and double-click did not snap the canvas back in Cut-out
- [x] **Double-click snaps the view back in a brushless mask tool.** Same root cause as Space, one gate further along: `InputController`'s dblclick was gated on `!mask.isMaskingMode`, so ANY mask mode kept it. Gated on `mask.paintEnabled` now. Reaches the image workspace's Detect/Points/Text/Adjust/Composite too, which is wider than this card and is called out in the commit
- [x] **The Mask Brush highlights the SAME region Cut-out does - and the words were not the bug.** The toggle DOES carry (opacity, `inverted`, `bwView` identical in both tools, measured on the canvas in all four directions). The PICTURE did not: the store holds "what stays", Cut-out displays `255 - alpha` so the highlight marks what GOES, the brush drew the raw mask. Fix: `MpiCanvas` gained `displayComplement` - the primitive that was missing (`displayInverted` only RECOLOURS, which is the documented reason Cut-out had to flip its bitmap). `MpiGifViewer` owns the flag so both tools share it and swaps paint/erase under it. THE STORE IS UNTOUCHED: the brush keeps its real layers and saves them back unchanged
- [x] Three traps, each hit once and each now in `docs/masking-sam3-gif.md`: Cut-out must NOT complement (its override is flipped already - the first attempt shipped that double flip and only measuring caught it); the flip is decided per frame VISIT and only when the frame already HAS a mask (the complement of nothing is the whole frame); paint/erase swap or the stroke shrinks what the cursor is over
- [x] Verify (third pass): `lint:components` clean; `node --test` 1419 pass / 0 fail; full desktop suite 134 passed / 4 failed - three the known peer frame-store bug (gif-cutout:446, gif-timing:73, gif-transform:56) and the fourth a live peer's in-flight uncommitted gallery work (claim 3f8e2d51 holds MpiGalleryGrid + gallery-filter-panel.spec.js; it passes in isolation). Both guards PROVEN RED first - the dblclick one with `InputController.js` from HEAD (the scale stayed at 51), the display one with all four source files from HEAD (the brush highlighted the disc while Cut-out highlighted the background, which is exactly his screenshots 1 and 2)
- [x] **A Mask Brush stroke reads the same in Cut-out (Fabio's screenshot, 2026-09-19).** Spec written FIRST and watched go red with his exact picture (hole in the tint, subject untouched). The store was right; Cut-out's canvas still carried the brush's `subtract` layer under an override that is composed already. First override after a mount reloads through the override branch; `_loadEditFrame` serialised. Proven red on HEAD's viewer
- [x] **Play in the Mask Brush keeps the highlight on what goes** - the handoff's "unproven" item, measured on a screenshot and found BROKEN (highlight on the disc one frame in). `_setPlayingTint()` + CSS `--complement`. Proven red on HEAD's viewer
- [ ] **Fabio's eye pass on the WHOLE workspace (nothing closes before it).** Not yet seen: Cut-out's strip and the new `Mask Preview` section, the output preview pane, Shift-select on the frame strip, Space playing in Cut-out
